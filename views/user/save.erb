<!DOCTYPE html>
<%#include "objects/user.h" %>
<% tfetch(QVariantMap, user); %>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= controller()->name() + ": " + controller()->activeAction() %></title>
</head>
<body>
<p style="color: red"><%=$ error %></p>
<p style="color: green"><%=$ notice %></p>

<h1>Editing User</h1>

<%== formTag(urla("save", user["username"]), Tf::Post) %>
  <p>
    <label>Username<br /><input type="text" name="user[username]" value="<%= user["username"] %>" readonly="readonly" /></label>
  </p>
  <p>
    <label>Password<br /><input type="text" name="user[password]" value="<%= user["password"] %>" /></label>
  </p>
  <p>
    <input type="submit" value="Save" />
  </p>
</form>

<%== linkTo("Show", urla("show", user["username"])) %> |
<%== linkTo("Back", urla("index")) %>
</body>
</html>
